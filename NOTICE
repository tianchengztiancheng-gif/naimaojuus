gal-engine —— 第三方来源与署名
================================

本仓库自己的代码按 MIT 发布（见 LICENSE）。下面列出所有不属于我们的东西：
它们从哪来、什么条款、在仓库里的什么位置。


一、随仓库分发的第三方代码
--------------------------

### gpt-tokenizer

  文件    core/vendor/gpt-tokenizer-cl100k_base.js
          core/vendor/gpt-tokenizer-o200k_base.js
          core/vendor/gpt-tokenizer-LICENSE.txt
  来源    npm 包 gpt-tokenizer 2.9.0，官方 dist/ 里的 UMD 构建，**原样未改**
  作者    Bazyli Brzoska
  条款    MIT（全文见 core/vendor/gpt-tokenizer-LICENSE.txt）
  用途    core/tokens.js 的真实 BPE 分词；删掉这两个文件会自动回退粗估，不报错


二、参考过但没有复制代码的项目
------------------------------

### KaiTuoYiShi （开拓遗失）

  https://github.com/LingYuYue1/KaiTuoYiShi
  条款  MIT © 2026 LingYuYue1

  core/imagegen.js 的 NovelAI 调用流程 —— 端点选择、v4_prompt 的多角色结构、
  centers 定位、返回 ZIP 的解包思路 —— 是照着它的实现重写的。没有直接复制文件，
  但设计来源是它，这里明确署名。
  （我们比它多做的：NAI 返回的是 **streaming zip**，local header 里的长度是 0，
    必须读中央目录才拿得到真实长度。core/imagegen.js 用三级兜底处理这件事。）

  v5.21 又对照它（2026-09-12 的 2d5760b）补了一批兼容性，同样是按思路重写、没有复制文件：
  - core/prompt.js   prompt_order 按 character_id 100001 取组；{{if}}/{{else}}、{{.变量}}/{{$变量}} 简写、
                     {{trim::}}/{{upper::}}/{{lower::}} 等宏（参考 utils/macroEngine.ts、tavernMessageChainBuilder.ts）
  - core/regex.js    预设正则的存放路径表（含 SPreset.RegexBinding）、snake_case 字段、对象映射、
                     抗空回 <Q>…</WF> / 抗截断 <math>…</math> 占位清理（参考 tavernRegexProcessor.ts、textSanitizer.ts）
  - core/api.js      测试连接的随机校验码、模型列表多候选地址、兼容响应解析（分段 content / data 信封 /
                     Anthropic 格式 / Responses）、DeepSeek 前缀续写、参数不兼容降级
                     （参考 apiTools.ts、connectionTestPolicy.ts、openAICompatibleModels.ts、chatCompletionClient.ts）
  - core/engine.js   空回自动重试一次（参考 hooks/useGame/sendWorkflow.ts）
  - core/savetree.js 与存档界面   存档树（节点 / 父节点 / 分支、自动与手动节点、整树导出删除、存档目录索引），
                     版式参考 components/features/SaveLoad/SaveLoadModal.tsx、utils/saveTreeView.ts
  - app/app.js 重roll            回合前快照 + 回滚、重写要求（参考 hooks/useGame.ts 的 handleReroll、
                     turnSnapshot.ts、sendWorkflow.ts 的 buildRerollGenerationGuard）

### Larimar（天青）

  条款  ⚠ **未声明**。没有 LICENSE 文件，README 里没有许可声明，也没有 package.json。

  早期版本参考过它的：提示词组装顺序（prompt_order + marker）、世界书激活协议、
  手机机身/灵动岛/主屏的视觉，以及一份 gpt-tokenizer 的 vendor 副本。

  **本仓库不再包含任何来自 Larimar 的文件。**
  - core/vendor/larimar-preset.js、larimar-regex.js 经查从未被 index.html 加载、
    代码里也没有引用（只有 core/engine.js 的一行旧注释提到），确认是死代码，已删除。
  - 原先从它那里取的 gpt-tokenizer 副本已换成 npm 官方构建。
    （顺带发现：它那份名为 cl100k_base 的文件里装的其实是 **o200k 的数据**，
      我们照文件名当 cl100k 用，导致文档里的分词误差表错了两版。见 docs/HANDOVER.md）

  仍受它影响的是设计与协议层面的东西，不是代码文本。如果 Larimar 作者认为
  哪部分仍构成实质性复制，请开 issue，我们会改写或移除。


三、**不在**本仓库里的东西
--------------------------

下面这些运行时需要，但因为版权原因不随仓库分发，需要你自己准备：

  juus 角色卡          作者 kikukiku。引擎的主要适配对象。
                       你得有自己的一份，在开场引导第一步载入。

  天青 / Larimar 预设   同上，自己准备。

  resource/juus/       从 juus 卡里抽出来的立绘 / 场景 / 头像 / 手机资源索引，
  resource/tianqing/   由 tools/build-juus.py 等脚本生成。已在 .gitignore 里。
                       **不装它们也能玩**：这些内容本来就在卡里，
                       core/cardres.js 会在载卡时当场读出来。
                       详见 resource/README.md。

  docs/立绘清单.md      juus 卡 3519 张立绘的清单，同样是卡的派生数据，不分发。

  碧蓝航线美术素材      © Yostar / 蛮啾网络。所有立绘、头像、场景图都托管在
                       第三方图床上，本仓库只在生成的索引里引用 URL，不托管图片，
                       而那些索引也不随仓库分发。


四、测试用的合成数据
--------------------

test/fixtures/resource.js 是 tools/build-fixtures.mjs 生成的**合成**素材表：
结构照着真表，但所有 URL 都是 https://example.invalid/ 占位，只保留测试点名的
十来个角色名，手机帖子/热点是我们自己手写的。它不含任何卡作者的文案或素材 URL。
