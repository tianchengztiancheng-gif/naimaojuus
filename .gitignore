node_modules/
package-lock.json

# ── 角色卡 / 预设 / 世界书：都是别人的作品，不进仓库 ──
*.card.json
card.json
preset*.json
worldbook*.json
角色卡*.json
预设*.json
世界书*.json

# ── 从角色卡生成的素材表（见 resource/README.md）──
resource/juus/
resource/tianqing/
docs/立绘清单.md

# ── 本地产物 ──
*.zip
# 例外：183 字节的 zip 测试夹具，test-imagegen 要用（gitignore 不支持行尾注释，别合并这两行）
!tools/unit/nai.zip
*.log
.DS_Store
Thumbs.db
/tmp/
shots/

# ── 调查/探测用的一次性脚本 ──
# 上一轮有个 _css_probe3.js 被 `git add -A` 扫进了公开仓库。
# 临时脚本一律用下面这些前缀命名，就不会再误传。
_probe*
_tmp*
*-tmp.*
v[0-9]*.mjs

# ── Claude 桌面端放截图的地方，不进仓库 ──
Claude outputs/
